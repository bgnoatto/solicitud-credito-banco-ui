import { DescripcionEstadoPipe } from './descripcion-estado.pipe';

describe('DescripcionEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescripcionEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
